name: Mypy

on:
  workflow_call:

jobs:
  mypy:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Branch
        uses: actions/checkout@v3

      - name: Setup Python
        uses: ./.github/actions/setup-python

      - name: Mypy
        run: mypy .
